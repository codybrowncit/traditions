	<tr>
		<td><%= link_to user.name, user_path(user) %></td>
		<td><%= user.major %></td>
		<td><%= user.email %></td>
		<td>
			<%= link_to "Edit", edit_user_path(user), class:"btn btn-default" %>
			<%= link_to "Delete", user_path(user), method: 'delete',data: { confirm: "Are you sure you want to Delete this user"}, class:"btn btn-default" %>
		</td>
	</tr>